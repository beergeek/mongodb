{
  "puppet_task_version": 1,
  "supports_noop": false,
  "description": "A short description of this task",
  "parameters": {
    "hash_data": {
      "description": "Takes ma hash and returns it as JSON. The function in puppetlabs/stdlib are not available so we need this task",
      "type": "Hash"
    }
  }
}
